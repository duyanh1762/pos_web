import { LongClickDirective } from './long-click.directive';

describe('LongClickDirective', () => {
  it('should create an instance', () => {
    const directive = new LongClickDirective();
    expect(directive).toBeTruthy();
  });
});
